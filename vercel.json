{
  "version": 2,
  "buildCommand": "cd artifacts/api-server && node build.mjs",
  "builds": [
    { "src": "artifacts/api-server/api/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/artifacts/api-server/api/index.ts" }
  ]
}